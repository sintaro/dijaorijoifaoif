import React from 'react';
import { StyleSheet } from 'react-native';

var styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#1832ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
})

module.exports = styles